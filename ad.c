#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define BOYUT 10

enum yer
{
    bos,
    teklil,
    ikilik,
    ucluk,
    dortluk,
    bomba,
};
void reset_board(enum yer tahta[BOYUT][BOYUT])
{
    for (int i = 0, i < BOYUT, i++)
    {

        for (int a = 0, a < BOYUT, a++)
        {
        }
    }
}
int main()
{
    enum yer board[BOYUT][BOYUT];
    reset_board(board);

}